import React from 'react';
import { View } from 'react-native';
import { Button } from 'react-native-elements';

const Deconect = () => {
    return (
        <View>
            <Button />
        </View>
    );
};

export { Deconect };
